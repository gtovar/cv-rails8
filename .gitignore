# See https://help.github.com/articles/ignoring-files for more about ignoring files.
#
# Temporary files generated by your text editor or operating system
# belong in git's global ignore instead:
# `$XDG_CONFIG_HOME/git/ignore` or `~/.config/git/ignore`
# See https://help.github.com/articles/ignoring-files for more about ignoring files.

# === Archivos generados por editores / sistema operativo ===
*.DS_Store
*.swp
*.keep

# === Configuración local y secretos ===
.env
.env.*
!.env.sample
/config/master.key

# === Configuración de bundler ===
/.bundle

# === Archivos de log y temporales ===
/log/*
/tmp/*
!/log/.keep
!/tmp/.keep

# Ignorar pidfiles, pero mantener la carpeta
/tmp/pids/*
!/tmp/pids/
!/tmp/pids/.keep

# === Archivos de almacenamiento y SQLite (desarrollo) ===
/storage/*
!/storage/.keep
/tmp/storage/*
!/tmp/storage/
!/tmp/storage/.keep
db/*.sqlite3
cv_development

# === Configuración local no oficial ===
config/database.multiple.yml
logfile

# === Assets compilados (si usas sprockets) ===
/public/assets

# === Ignorar tareas y semillas personalizadas (si son temporales) ===
lib/tasks/*
lib/seeds/*

# === Tests / Fixtures / Archivos de prueba ===
test/*
test/fixtures/*.yml
*.log

